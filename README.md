🚀 FAST DNS HALOTEL TZ 🇹🇿

Copy the script bash to your VPS to run:

```bash
sudo apt install -y curl && \
curl -fsSL https://raw.githubusercontent.com/amokhansecretprojects-beep/DNSTT-MODED/main/DNS%20MODED/moded.sh -o moded.sh && \
chmod +x moded.sh && \
sed -i 's/\r$//' moded.sh && \
./moded.sh
